Open talk_example.sas and edit the first line to point to the containing directory. 

The first five lines read in data created externally that are the summary statistics needed, with the correct variable labels in long format. It is expected that in a real study seperate blocks of code will produce these from raw data.

If run correctly then it will produce 3 new files within the same directory: simple0.xml  simple.xml table_eudract.xml. The final one being able to be uploaded into eudraCT. Compare to the files in the "Reference Output" directory, which is what your output should be. 

